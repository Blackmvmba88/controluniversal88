# frozen_string_literal: true

source "https://rubygems.org"

# Ruby version (adjust based on your environment)
ruby ">= 3.0.0"

# ryby gem - the main requirement from the issue
gem "ryby", "~> 0.1.2"

# Additional dependencies for a web server implementation (similar to Python/Node versions)
gem "sinatra", "~> 3.0"
gem "sinatra-contrib", "~> 3.0"
gem "puma", "~> 6.0"
gem "faye-websocket", "~> 0.11"

# Development dependencies
group :development, :test do
  gem "rspec", "~> 3.12"
  gem "rubocop", "~> 1.50"
end
